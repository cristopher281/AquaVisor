## Ejemplo de variables de entorno para el servidor AcuaVisor
# COPIA ESTE ARCHIVO A ".env" Y RELLENA LOS VALORES REALES (NO COMMITTEAR .env)

# Variables de entorno (ejemplo)
# Puerto en el que corre el servidor (opcional)
PORT=4000
# Puerto en el que corre el servidor (opcional)
PORT=4000
